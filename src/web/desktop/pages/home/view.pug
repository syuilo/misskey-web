extends ../../../base

block title
	| Misskey

block head
	meta(name='description', content='Misskey Home')
	link(rel='stylesheet', href= config.urls.resources + '/desktop/pages/home/style.css')
	script(src= config.streamingUrl + '/socket.io/socket.io.js')
	script(src= config.urls.resources + '/desktop/pages/home/script.js')

block body
	mk-ui
		main
